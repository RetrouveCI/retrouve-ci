/** Below this, a search would match half the base. */
export const PALETTE_MIN_QUERY = 2

/** A palette points somewhere; the lists are where one reads more. */
export const PALETTE_RESULTS_PER_KIND = 5
