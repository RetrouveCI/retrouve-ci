import type { NotificationType } from './types/notification.types'

export const NOTIFICATION_TYPES: NotificationType[] = ['match_found']

export const DEFAULT_PAGE_SIZE = 20
export const MAX_PAGE_SIZE = 100
